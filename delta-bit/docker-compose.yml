services:

  gui:
    build: ./GUI
    user: "${HOST_UID}:${HOST_GID}"
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    working_dir: /app
    command: python data/gui.py

  tf:
    build: ./TF
    user: "${HOST_UID}:${HOST_GID}"
    ports:
      - "9000:9000"
    volumes:
      - ./data:/app/data
    working_dir: /app

  tools:
    build: ./tools
    user: "${HOST_UID}:${HOST_GID}"
    ports:
     - "8000:8000"
    volumes:
      - ./data:/data
    working_dir: /data
